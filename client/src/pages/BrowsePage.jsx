export default function BrowsePage() {
  return (
    <p style={{ padding: 'var(--section-pad)', fontFamily: 'var(--font-body)', color: 'var(--text-primary)' }}>
      Browse — coming in slice 4
    </p>
  )
}
